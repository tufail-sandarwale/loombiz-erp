export const environment = {
  production: true,
  enableMsal: true,
  apiHostUrl: window['env']['apiUrl'],
  uploadFileSizeInMB: window['env']['uploadFileSizeInMB'],
  uploadFileTypes: window['env']['uploadFileTypes'],
  uploadFileExtensions: window['env']['uploadFileExtensions']
};
