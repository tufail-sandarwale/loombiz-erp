import { RvDisplayPricePipe } from './rv-display-price.pipe';

describe('RvDisplayPricePipe', () => {
  it('create an instance', () => {
    const pipe = new RvDisplayPricePipe();
    expect(pipe).toBeTruthy();
  });
});
